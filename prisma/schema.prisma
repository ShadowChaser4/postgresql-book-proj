// *** GENERATED BY PRISMIX :: DO NOT EDIT ***
datasource db {
  provider = "postgresql"
  url      = env("CONNECTION_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Book {
  id          Int     @id @default(autoincrement())
  name        String
  description String?
  authorId    Int
  author      Author  @relation(name: "AuthorToBook", fields: [authorId], references: [id])
}

model Author {
  id         Int     @id @default(autoincrement())
  book       Book[]  @relation(name: "AuthorToBook")
  bio        String?
  firstName  String
  middleName String?
  lastName   String
}
