
model Book {
  id          Int     @id @default(autoincrement())
  name        String
  description String?
  authorId    Int
  author      Author  @relation(fields: [authorId], references: [id])
}

model Author {
  id    Int   @id @default(autoincrement())
  book  Book []
}